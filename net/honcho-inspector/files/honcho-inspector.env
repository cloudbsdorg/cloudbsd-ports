# Honcho Inspector secrets (mode 0640 root:www).
# Set HONCHO_CRYPTO_KEY to a stable base64 value (e.g.
# `openssl rand -base64 32`) and restart the service. Without
# this key, encrypted profile API keys will not survive a restart.
HONCHO_CRYPTO_KEY=
