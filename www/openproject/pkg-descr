OpenProject is open-source project management: work packages, Gantt,
boards, wiki, and time tracking. This CloudBSD port builds official
OpenProject 17.7.2 from source (native gems + frontend assets) and
runs Puma on 127.0.0.1:3000 and a separate good_job worker, behind nginx.

OpenProject reads environment variables, not JSON. The runtime file
is /usr/local/etc/cloudbsd/openproject/openproject.env. Optional JSON
in that directory can generate the env file.

Default track.cloudbsd.org layout:

  Puma          127.0.0.1:3000
  PostgreSQL    16 or 17 (port option, default 17)
  memcached     127.0.0.1:11211
  nginx         TLS terminator, X-Forwarded-Proto https
  Host          track.cloudbsd.org, OPENPROJECT_HTTPS=true

WWW: https://www.openproject.org/
