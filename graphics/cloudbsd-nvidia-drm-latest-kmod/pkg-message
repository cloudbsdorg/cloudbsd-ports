Modesetting must be enabled to use nvidia-drm.ko for graphics.

hw.nvidiadrm.modeset=1

This must be set before loading nvidia-drm.ko (e.g. in /boot/loader.conf).

CloudBSD tip: cloudbsd-gpu-detect (sysutils/cloudbsd-gpu-autoload).
